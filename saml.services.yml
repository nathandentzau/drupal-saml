services:
  saml.entity_descriptor_factory:
    class: Drupal\saml\Factory\Model\Metadata\EntityDescriptorFactory
    arguments:
      - '@event_dispatcher'

  saml.message_factory:
    class: Drupal\saml\Factory\Model\Protocol\SamlMessageFactory
    arguments:
      - '@event_dispatcher'
